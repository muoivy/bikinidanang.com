# features

Chứa các module theo use-case (ví dụ: product-listing, product-detail, cart, checkout).

Quy ước:
- Mỗi feature có thể có `components/`, `hooks/`, `services/`, `types/` riêng.
- Không import vòng giữa các feature.
