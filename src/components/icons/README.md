# Icon system

Use SVG components only for now (site has few icons).

- Use `<Icon name="..." />` from `src/components/icons`.
- Icons inherit text color via `currentColor`.
- Keep system minimal; revisit optimization strategy later when icon count grows.
